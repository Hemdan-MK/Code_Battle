// import { useSelector } from 'react-redux';
// import { Navigate } from 'react-router-dom';

// export const RequireSignup= ({ children }) => {
//   const isSignupDone = useSelector((state) => state.auth.isSignupDone);
//   return isSignupDone ? children : <Navigate to="/signup" />;
// };

// export const RequireSignOTP = ({ children }) => {
//   const isOTPVerified = useSelector((state) => state.auth.isOTPVerified);
//   return isOTPVerified ? children : <Navigate to="/signup" />;
// };

